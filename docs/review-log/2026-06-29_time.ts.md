# Review Log: time.ts

## Metadata

- Review Date: 2026-06-29
- Commit: 65376b2
- Path: src/util/time.ts

## Code Diff

```diff
+/**
+ * Source of UTC timestamps used by tracking components.
+ */
+export interface Clock {
+  /**
+   * Returns the current time as a UTC ISO8601 timestamp.
+   *
+   * @returns Current timestamp in `Date.prototype.toISOString()` format.
+   */
+  nowIso(): string;
+}
+
+/**
+ * Date-backed clock used by the extension at runtime.
+ */
+export const systemClock: Clock = {
+  nowIso(): string {
+    return new Date().toISOString();
+  },
+};
```

コミット `65376b2`「feat: セッション計測を実装」で新規追加。
`Clock` インターフェースで時刻取得を抽象化し、`systemClock` を実装として提供する。
`SessionTracker` の `constructor` でデフォルト引数として注入され（`clock: Clock = systemClock`）、テスト時にモック時計へ差し替え可能にする定石パターン。
`nowIso()` が返すISO文字列はそのまま Repository に保存されるため、文字列で返す設計は用途に合致している。

## Questions from User

- なし（ユーザーはコードの挙動を理解済み）

## Problems and fixes

- P1: `systemClock` が `Object.freeze` されていない。`const` は変数の再代入は防ぐが、`systemClock.nowIso = ...` のようなメソッドの書き換えは防げない。アプリ全体で共有されるシングルトンのため、誤った上書きは全コンシューマに波及する。
  - F1: 修正は**見送り**。現状のコードに実害はなく、保険レベルの対策であるため、現段階では不要とユーザーが判断。`Object.freeze({...})` でのハードニングは将来必要になれば検討する。

| Problem | Finder |
| ------- | ------ |
| P1      | Claude |

> [!NOTE]
> P: Problem, F: fix
